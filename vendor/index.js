$(document).ready(function() {
    // $(document).on('click', 'a', function(event) {
    //     var href = $(this).attr('href');
    //     var target = $(this).attr('target');
    //     if (target[0] != '_') {
    //         event.preventDefault();
    //         console.log($('[name="' + target + '"]'));
    //         $('[name="' + target + '"]').attr('src', href);
    //     }
    // });
});